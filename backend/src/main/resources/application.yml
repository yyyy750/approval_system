server:
  port: 8080

spring:
  datasource:
    # 数据库连接 URL（请确保已创建 approval_system 数据库）
    url: jdbc:mysql://117.72.60.69:3306/approval_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: rainknows
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      # 文件上传大小限制
      max-file-size: 50MB
      max-request-size: 50MB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  # JWT 密钥（生产环境请更换为更安全的密钥）
  secret: approval-system-jwt-secret-key-2026-must-be-32-chars
  # Token 过期时间：24小时
  expiration: 86400000

# 文件上传配置
file:
  upload-dir: D:/uploads/approval-system
  access-path: /files
  allowed-types: doc,docx,xls,xlsx,ppt,pptx,pdf,txt,jpg,jpeg,png,gif,zip,rar
  max-size: 52428800